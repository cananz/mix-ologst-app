<h1>Ingredients</h1>

<ul>
  <% @ingredients.each do |ing| %>
    <li><%= link_to ing.name, ingredient_path(ing) %></li>
  <% end %>
</ul>

<%= link_to "Add a new ingredient", new_ingredient_path %><br>
<%= link_to "Check out our cocktail menu", cocktails_path %>